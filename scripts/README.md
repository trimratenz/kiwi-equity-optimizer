# Scripts

`form-state-smoke-test.mjs` validates the mortgage form reducer without rendering React. It covers the regression where adding a tranche could make previously entered values appear to reset.
